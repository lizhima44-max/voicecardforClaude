# 让你的 AI 用声音回你

> 这页是写给**不写代码的人**的。技术细节在 [TUTORIAL.md](TUTORIAL.md)，那份是给 AI 读的。

不是把文字丢进一个朗读软件。是你跟 AI 说话，它**在对话里**回你一条能点开播放的语音消息——像微信那样，有播放键、有波形、有时长。声音可以是你克隆的任何人的。

整套东西已经做好了，开源的。你不需要看懂任何一行代码——**把这个仓库甩给你的 AI，让它替你装。**

这页说的是：你要准备什么、跟 AI 怎么说、卡住了怎么办。

---

## 你要准备三样

都不难拿，加起来半小时之内。

**一个一直开着的地方**
让程序跑着等 AI 来叫它。最便宜的云服务器就够，一个月十几块；用自己的电脑也行，但关机就断。

**一个配音账号**
**ElevenLabs**（国外，克隆最像，多语种强）或**火山引擎**（国内，中文最自然，网络友好）。二选一，都有免费额度，日常聊天够用很久。

**一个网址**
让 AI 能找到你的程序。**不用买域名**——教程里有免费方案，一条命令就能给你一个带 https 的地址。

---

## 把活交给 AI

### 1. 先把仓库给它

开一个能读 GitHub 的 AI——Claude Code、Cursor、或者任何接了网的助手。把下面这段原样发过去：

```
帮我装一个语音条 MCP 服务器，代码和教程都在这里：
https://github.com/lizhima44-max/voicecardforClaude

先读 TUTORIAL.md，然后一步一步带我做。
我不懂代码，每一步要我干什么、要我提供什么，你直接说，我照做。
```

它会先问你两件事：用哪家配音、准备装在哪。按上面"三样"里你选的回答就行。

### 2. 把钥匙交给它

AI 会让你去配音平台拿几串字符（API key、音色 ID 这类）。平台上都是复制按钮，点了粘给它。

> ⚠️ **这几串东西等于你的钱包。** 只给你自己在用的 AI，别发到群里、别贴在公开的地方。
> 万一不小心发出去了，回平台把那一串作废、重发一个新的——这是标准操作，不丢人。

### 3. 验收

装完之后，在 claude.ai 的**设置 → 连接器**里会多出一个。然后在对话里说一句：

```
用语音跟我说句晚安
```

对话里直接冒出一条语音条，点播放能出声——就是成了。出不来别自己瞎猜，看下一节。

---

## 卡住了怎么说

这几样是最常撞上的。你不用懂它们什么意思，**把右边那句原样转给 AI**，它就知道去改哪了。

| 你看到的 | 转给 AI 的话 |
|---|---|
| 对话里只有一堆代码字符，没有语音条 | 卡片没渲染出来。检查资源的 mime_type 有没有带 `;profile=mcp-app`、工具 meta 里有没有 `ui/resourceUri`、以及 initialize 有没有声明 `io.modelcontextprotocol/ui` 能力。 |
| 语音条出来了，但一直显示加载失败、时长是 0:00 | 音频被 iframe 的 CSP 拦了。检查 `AUDIO_BASE_URL` 的域名是不是一字不差地进了资源 meta 的 `connectDomains`，以及它是不是 https。 |
| 报 `Failed to load the MCP app` | 八成是工具返回值太大（别把 base64 音频塞进返回值），或者 meta 里混进了 `domain` 字段。两个都查一下。 |
| 反复提示 `Missing session ID` | 把环境变量 `MCP_STATELESS` 设成 1，然后重启服务。 |
| 装的时候报 `No module named 'mcp.server.fastmcp'` | 装到 MCP SDK 2.x 了，那版把 FastMCP 改名了。重装成 `"mcp>=1.9.0,<2"`。 |
| 工具列表里明明有，就是调不动、老是超时 | 同步工具堵住了事件循环。确认 `server.py` 里那段把同步工具丢进线程池的补丁还在。 |
| 点下载按钮完全没反应 | 这是宿主 iframe 的 sandbox 限制，不是代码写错。卡片已经做了兜底：点下载会就地展开链接，复制出来粘到浏览器里打开。 |

教程里还有十几条，连同每一条**真正的原因**——不只是症状。AI 读得懂那张表，卡住时让它回去翻。

---

## 想用自己的声音

两家平台都支持克隆。录**一到十分钟**的干净人声传上去，几分钟后给你一个音色 ID，填进配置就换成那个声音了。

录的要求很低：**安静、语速正常、没有背景音乐**，手机录就行。唯一要躲的是**有回声的房间**——浴室、空客厅，克隆出来会闷。

> 用别人的声音之前，**先问过本人**。这事没有技术门槛，但有伦理门槛。

---

## 要花多少

| | |
|---|---|
| 服务器 | 十几块 / 月起；用自己电脑则 0 元 |
| 配音 | 免费额度日常够用（ElevenLabs 约一万字符 / 月） |
| 网址 | 0 元（免费隧道） |
| 你的时间 | 顺利 30 分钟，卡住大概两小时 |

---

## 仓库里有什么

- **`single-file/voice_card_mcp.py`** —— 单文件版，一个文件跑起来，适合先看效果
- **`server.py` / `tts.py` / `voice_card.html`** —— 完整版，适合长期用
- **`TUTORIAL.md`** —— 给 AI 读的详细教程，从装 Python 讲起，带完整的报错对照表
- **`CREDITS.md`** —— 借鉴了谁，逐条写清

---

## 出处

语音条这个形态，以及"卡片必须显式声明 CSP 白名单"这个关键发现，参考自
[garan0613/voice-mcp](https://github.com/garan0613/voice-mcp)（MIT 许可）。
那是一份 TypeScript 实现，本项目是独立重写的 Python 版本，没有复制其代码。
[CREDITS.md](CREDITS.md) 里逐条写清了借了什么、没借什么。

本项目同样 MIT 许可——随便拿去用、改、发布，保留版权声明即可。
