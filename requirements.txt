# MCP Python SDK。
# 🚨 必须钉在 1.x：2.0 起 FastMCP 改名成了 MCPServer，API 也变了，
#    直接 pip install mcp 会装到 2.x，这份代码就 import 不到 FastMCP。
mcp>=1.9.0,<2
# HTTP 客户端，调 TTS API 用
requests>=2.31.0
# ASGI 服务器
uvicorn>=0.30.0
# 读 .env 文件（没装也能跑，那就只认系统环境变量）
python-dotenv>=1.0.0
